[package]
name = "boo-language"
version.workspace = true
edition.workspace = true

[lib]
bench = false

[dependencies]
boo-core = { path = "../core" }

proptest = "1.4.0"
proptest-derive = "0.4.0"

[dev-dependencies]
anyhow = "1.0.79"

[package.metadata.cargo-machete]
ignored = ["proptest"]
